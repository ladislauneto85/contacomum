{
    "builds": [{
      "src": "cadastro_comum/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.13.4" }
    }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "cadastro_comum/wsgi.py"
      }
    ]
}